name: Daily Briefs

on:
  schedule:
    # 11:00 UTC = 08:00 Argentina (UTC-3).
    - cron: "0 11 * * *"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # --- Informe IA & Gobernanza ---
  brief-ia:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install deep-translator
      - name: Generar informe IA
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}
        run: python3 brief.py --out brief.md --out-es brief_es.md --html ia.html --html-es ia_es.html --json brief_items.json
      - uses: actions/upload-artifact@v4
        with:
          name: brief-ia
          path: |
            brief.md
            brief_es.md
            ia.html
            ia_es.html
            brief_items.json
          retention-days: 30

  # --- Informe Ciberseguridad ---
  brief-cyber:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install deep-translator
      - name: Generar informe Cyber
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_TO_CYBER: ${{ secrets.MAIL_TO_CYBER }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}
        run: python3 brief_cyber.py --out cyber_brief.md --out-es cyber_brief_es.md --html cyber.html --html-es cyber_es.html --json cyber_items.json
      - uses: actions/upload-artifact@v4
        with:
          name: brief-cyber
          path: |
            cyber_brief.md
            cyber_brief_es.md
            cyber.html
            cyber_es.html
            cyber_items.json
          retention-days: 30

  # --- Herramientas GitHub: Ofensiva e Inteligencia ---
  brief-github-ofensiva:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install deep-translator
      - name: Generar herramientas Ofensiva e Inteligencia
        env:
          GITHUB_TOKEN: ${{ secrets.GH_SEARCH_TOKEN }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}
        run: python3 brief_github.py --set ofensiva --out github_ofensiva_brief.md --out-es github_ofensiva_brief_es.md --html github_ofensiva.html --html-es github_ofensiva_es.html --json github_ofensiva_items.json
      - uses: actions/upload-artifact@v4
        with:
          name: brief-github-ofensiva
          path: |
            github_ofensiva_brief.md
            github_ofensiva_brief_es.md
            github_ofensiva.html
            github_ofensiva_es.html
            github_ofensiva_items.json
          retention-days: 30

  # --- Herramientas GitHub: Defensa / DFIR ---
  brief-github-defensa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install deep-translator
      - name: Generar herramientas Defensa
        env:
          GITHUB_TOKEN: ${{ secrets.GH_SEARCH_TOKEN }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}
        run: python3 brief_github.py --set defensa --out github_defensa_brief.md --out-es github_defensa_brief_es.md --html github_defensa.html --html-es github_defensa_es.html --json github_defensa_items.json
      - uses: actions/upload-artifact@v4
        with:
          name: brief-github-defensa
          path: |
            github_defensa_brief.md
            github_defensa_brief_es.md
            github_defensa.html
            github_defensa_es.html
            github_defensa_items.json
          retention-days: 30

  # --- Herramientas GitHub: IA, Gobernanza y Datos ---
  brief-github-iatools:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install deep-translator
      - name: Generar herramientas IA y Gobernanza
        env:
          GITHUB_TOKEN: ${{ secrets.GH_SEARCH_TOKEN }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}
        run: python3 brief_github.py --set ia --out github_iatools_brief.md --out-es github_iatools_brief_es.md --html github_iatools.html --html-es github_iatools_es.html --json github_iatools_items.json
      - uses: actions/upload-artifact@v4
        with:
          name: brief-github-iatools
          path: |
            github_iatools_brief.md
            github_iatools_brief_es.md
            github_iatools.html
            github_iatools_es.html
            github_iatools_items.json
          retention-days: 30

  # --- Informe ejecutivo + email unico ---
  resumen:
    needs: [brief-ia, brief-cyber, brief-github-ofensiva, brief-github-defensa, brief-github-iatools]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Descargar informes
        uses: actions/download-artifact@v4
        with:
          path: _reports
          merge-multiple: true
      - name: Generar informe ejecutivo
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}
        run: python3 brief_exec.py --out resumen.html --out-md _reports/_exec.md _reports/brief_items.json _reports/cyber_items.json _reports/github_ofensiva_items.json _reports/github_defensa_items.json _reports/github_iatools_items.json
      - uses: actions/upload-artifact@v4
        with:
          name: resumen
          path: resumen.html
          retention-days: 30
      - name: Enviar email unico (ejecutivo + los tres informes)
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
        # El _exec.md va primero -> el email arranca con el resumen ejecutivo.
        # Para recibir las noticias traducidas al espanol, cambia los .md por sus
        # variantes _es.md (brief_es.md, cyber_brief_es.md, github_brief_es.md).
        run: python3 send_digest.py _reports/_exec.md _reports/brief.md _reports/cyber_brief.md _reports/github_ofensiva_brief.md _reports/github_defensa_brief.md _reports/github_iatools_brief.md

  # --- Deploy a GitHub Pages ---
  deploy:
    needs: [brief-ia, brief-cyber, brief-github-ofensiva, brief-github-defensa, brief-github-iatools, resumen]
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Descargar artifacts
        uses: actions/download-artifact@v4
        with:
          path: _site
          merge-multiple: true
      - name: Copiar portal como index
        run: cp portal.html _site/index.html
      - name: Configurar Pages
        uses: actions/configure-pages@v5
      - name: Subir sitio
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Publicar en GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
